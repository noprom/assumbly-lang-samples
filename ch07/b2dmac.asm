RETURN	MACRO
	MOV	AX,4C00H
	INT	21H
	ENDM

	
BIN_DEC	MACRO	ASC,BIN
	LOCAL	L1,L2
	
;16位二进制数－－>十进制数的ASCII码，第一个参数为
;五个字节的十进制数缓冲区，第二个参数为要转换的二进制数

	LEA	SI,ASC+4
	MOV	AX,BIN
	MOV	CX,10
L1:
	CMP	AX,0
	JE	L2
	MOV	DX,0
	DIV	CX
	OR	DL,30H
	MOV	[SI],DL
	DEC	SI
	JMP	SHORT	L1
L2:
	NOP
	ENDM


DISP	MACRO	ASC

;;显示十进制数
;参数ASC为十进制数缓冲区

	MOV	AH,9
	LEA	DX,ASC
	INT	21H
	ENDM


DATA	SEGMENT
	 BUF	DB  5 DUP(0), '$'
DATA	ENDS


CODE	SEGMENT
	ASSUME  CS:CODE, DS:DATA
MAIN	PROC	FAR

	MOV	AX,DATA
	MOV	DS,AX
	
	.XALL
	BIN_DEC	BUF, 0EEH

	.LALL
	DISP	BUF
	
	.SALL
	RETURN
	
MAIN	ENDP
CODE	ENDS
	END	MAIN